name: Offline image release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: Existing v-prefixed release tag to rebuild
        required: true
        type: string

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          ref: ${{ inputs.version || github.ref }}
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Verify release
        run: |
          go test ./...
          cd frontend
          npm ci
          npm run build
      - name: Build offline image archive
        env:
          RELEASE_VERSION: ${{ inputs.version || github.ref_name }}
        run: sh ./scripts/release-image.sh "$RELEASE_VERSION"
      - name: Publish GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_VERSION: ${{ inputs.version || github.ref_name }}
        run: gh release create "$RELEASE_VERSION" "dist/trace-$RELEASE_VERSION.tar.gz" --title "Trace $RELEASE_VERSION" --generate-notes --verify-tag
